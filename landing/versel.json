{
    "builds":[{
        "src": "test_project/wsgi.py",
        "user": "@vercel/python",
        "config": { "maxLambdaSize": "15mb", "runtime": "python3.13.1"}
    }],
    "routes":[
        {
            "src": "/(.*)",
            "dest": "test_project/wsgi.py"
        }
    ]
}